<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Probe only for now: presenting as a Bluetooth gamepad to another machine. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Being a Low Energy gamepad means advertising one. -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <queries>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher_famicom"
        android:theme="@style/Theme.SidePad"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask" />

        <!-- Two launcher icons for the same screen; the app enables one at a time. Famicom is the default. -->
        <activity-alias
            android:name=".LauncherFamicom"
            android:targetActivity=".MainActivity"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_famicom"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".LauncherGameBoy"
            android:targetActivity=".MainActivity"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_gameboy"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".TriggerActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="dev.lbento.thorsidepad.SHOW" />
                <action android:name="dev.lbento.thorsidepad.HIDE" />
                <action android:name="dev.lbento.thorsidepad.TOGGLE" />
                <action android:name="dev.lbento.thorsidepad.EDIT" />
                <action android:name="dev.lbento.thorsidepad.STOP" />
                <action android:name="dev.lbento.thorsidepad.PANEL" />
                <action android:name="dev.lbento.thorsidepad.START" />
                <action android:name="dev.lbento.thorsidepad.GUIDE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".TriggerReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="dev.lbento.thorsidepad.SHOW" />
                <action android:name="dev.lbento.thorsidepad.HIDE" />
                <action android:name="dev.lbento.thorsidepad.TOGGLE" />
                <action android:name="dev.lbento.thorsidepad.EDIT" />
                <action android:name="dev.lbento.thorsidepad.STOP" />
                <action android:name="dev.lbento.thorsidepad.PANEL" />
                <action android:name="dev.lbento.thorsidepad.START" />
                <action android:name="dev.lbento.thorsidepad.GUIDE" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".FocusHandoffActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <service
            android:name=".pad.OverlayService"
            android:exported="false" />

        <service
            android:name=".pad.PadTileService"
            android:exported="true"
            android:icon="@drawable/ic_pad"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>
</manifest>
